import React from 'react';
import { Create as CreateCreature } from "../Components/Forms/Creature";

const Home = (props) => {
    return (
        <>
            <CreateCreature />
        </>
    );
};

export default Home;